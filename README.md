# Akramium

Releases of the Akramium browser for Linux x64.

Each release has the browser package (`akramium-<version>-linux-x64.tar.gz`), its sha256, and
`update.json` with its signature, which Akramium reads to update itself in the background.
